<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /  # Change this if CodeIgniter is in a subfolder

    # Redirect index.php URLs
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
